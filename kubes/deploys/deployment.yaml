apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-fe
  labels:
    app: nginx
    env: dev
spec:
  replicas: 4
  selector:
    matchLabels:
      app: nginx
      env: dev

  template:
    metadata:
      labels:
        app: nginx
        env: dev
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
        - name: monitoring-pod-exporter
          image: quay.io/prometheus/node-exporter:latest
          ports:
            - containerPort: 9100
